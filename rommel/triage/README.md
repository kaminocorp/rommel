# `triage/` — raw ideas

The dumping ground. New ideas, sketches, half-formed thoughts. No structure required — one markdown file per idea, named however you want. Files can fly around; the only rule is they live here until promoted to `plans/`.

Next stage: `plans/`.
