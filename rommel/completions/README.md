# `completions/` — finished work, not yet shipped

One md file per completed phase. The plan that drove the work lives next to the completion doc that summarises what landed.

Once the work has been git-pushed and is live in production, the pair moves to `archive/`.

Next stage: `archive/`.
