# Phase 7 — `pty.*` primitives (Completion)

Dogfooded pointer. Canonical completion doc lives at [`docs/completions/phase-7-pty.md`](../../docs/completions/phase-7-pty.md). Plan, also dogfooded, archived at [`rommel/archive/phase-7-pty-plan.md`](../archive/phase-7-pty-plan.md).

This card is promoted from `completions/` → `archive/` when the phase ships (git push), via a real `funnel.promote` call against the dogfooded directory — meta-dogfooding the same primitive Phase 6 introduced.
