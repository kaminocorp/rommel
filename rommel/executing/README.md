# `executing/` — what's being worked on right now

Active work. Ideally one file at a time — concurrent execution is allowed but discouraged in v1.

A file in this stage represents a commitment: someone (or some agent) is actively iterating against it. On finish, it moves to `completions/` along with a sibling `phase-N-<name>.md` completion doc.

Next stage: `completions/`.
