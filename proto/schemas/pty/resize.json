{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://rommel.dev/schemas/pty/resize.json",
  "title": "PtyResize",
  "description": "pty.resize — adjust PTY dimensions when the browser window or pane resizes. Daemon issues TIOCSWINSZ via pty.Setsize. Same cols/rows bounds as pty.open.",
  "$defs": {
    "PtyResizeRequest": {
      "title": "PtyResizeRequest",
      "type": "object",
      "required": ["pty_id", "cols", "rows"],
      "properties": {
        "pty_id": { "type": "string" },
        "cols": { "type": "integer", "minimum": 1, "maximum": 1000 },
        "rows": { "type": "integer", "minimum": 1, "maximum": 1000 }
      },
      "additionalProperties": false
    },
    "PtyResizeResponse": {
      "title": "PtyResizeResponse",
      "type": "object",
      "description": "Empty — the resize is a synchronous ioctl; the ack is enough.",
      "additionalProperties": false
    }
  },
  "oneOf": [
    { "$ref": "#/$defs/PtyResizeRequest" },
    { "$ref": "#/$defs/PtyResizeResponse" }
  ]
}
