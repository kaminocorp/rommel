{
  "name": "rommel",
  "version": "0.0.0",
  "private": true,
  "description": "Rommel monorepo root — pnpm workspace, devDeps only.",
  "packageManager": "pnpm@9.12.0",
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=9.0.0"
  },
  "scripts": {
    "lint": "make lint",
    "test": "make test",
    "build": "make build"
  },
  "devDependencies": {},
  "pnpm": {
    "overrides": {
      "@types/react": "^19.0.0",
      "@types/react-dom": "^19.0.0"
    }
  }
}
